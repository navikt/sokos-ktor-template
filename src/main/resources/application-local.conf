include "application.conf"

application {
    profile = LOCAL
}

# Peker mot mock-oauth2-server i docker-compose.yaml (issuerId = "default").
# Start den med: docker compose up
azureAd {
    clientId = "local-client-id"
    wellKnownUrl = "http://localhost:8081/default/.well-known/openid-configuration"
}
